import styled from '@emotion/styled'
import { Flex } from '../..'

export const StyledContainer = styled(Flex)(() => {
  return {
    padding: '1.5rem 1rem',
    fontSize: '2rem',
    lineHeight: '2.4rem',
    color: '#68615B',
  }
})
